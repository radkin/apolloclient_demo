import { SearchArtifactsPipe } from './search-artifacts.pipe';

describe('SearchArtifactsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArtifactsPipe();
    expect(pipe).toBeTruthy();
  });
});
